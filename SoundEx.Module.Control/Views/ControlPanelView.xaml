<UserControl x:Class="SoundEx.Module.Control.Views.ControlPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0"
                Width="50"
                Height="20"
                Margin="10"
                Command="{Binding AddFileCommand}"
                Content="添加文件" />
        <Button Grid.Row="1"
                Width="50"
                Height="20"
                Margin="10"
                Command="{Binding AddFileCommand}"
                Content="添加文件" />
        <Button Grid.Row="2"
                Width="50"
                Height="20"
                Margin="10"
                Command="{Binding AddFileCommand}"
                Content="添加文件" />






    </Grid>
</UserControl>
